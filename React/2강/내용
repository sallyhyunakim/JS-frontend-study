# 2.2
* React JS로 element 생성하기(유효한 HTML 태그이기만 하면 다 됨)
    script 안에
    const element = React.createElement("element"); 삽입
* body 안에 React element 가져다 놓는 방법
    ** render: React element를 가지고 HTML로 만들어 배치한다는 것(사용자에게 보여준다는 뜻)
* ReactDOM : React element를 가져다가 HTML로 바꾸는 역할을 함

#2.5
* JSX는 JavaScript를 확장한 문법

#2.6
* function Title() {
    return (
        <h3 id="title" onMouseEnter={() => console.log("mouse enter")}>
            Hello I'm a title
            </h3>
    );
}
======================
const Title = ()=> (
    <h3 id="title" onMouseEnter={() => console.log("mouse enter")}>
            Hello I'm a title
    </h3>
    );

* HTML 요소라면 소문자로, 내가 생성한거는 앞 문자 대문자로 시작